library(NLP)
library(tm)
setwd("~/Documents/datasciencecoursera/Capstone Project/data/en_US/") #Mac
sample_size <- 1000
path <- "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.twitter.txt"
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
scan(file = con, what = "char", nmax = 1)
scan(file = con, what = "char", nlines = 1)
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
rbinom(1, 1, prob = c(0.5, 0.5))
text_sample <- c()
1:nrow_text
i = 1
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
i = 2
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
i = 3
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
i = 4
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
readLines(con, i)
text_sample <- c()
j <- 0
i = 1
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
j <- j + 1
i = 2
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
j <- j + 1
i = 3
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
j <- j + 1
i = 4
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
i - j
i-1
scan(file = con, what = "char", nlines = 1, skip = i-1)
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1)
readed_line
c(text_sample, readed_line)
text_sample <- c(text_sample, readed_line)
i = 5
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1)
text_sample <- c(text_sample, readed_line)
text_sample
text_sample <- c()
i = 1
test <- rbinom(1, 1, prob = c(0.5, 0.5))
test == 0
test == 1
n_sample <- n_sample + 1
n_sample <- 0
n_sample <- n_sample + 1
n_sample
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1)
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
}
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1)
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
text_sample
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1, quote = "")
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
text_sample
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample
readed_line
readLines(readed_line)
text_sample
text_sample
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1, quote = "")
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
text_sample
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
paste(sample)
paste(sample, collapse = " ")
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1, skip = i-1, quote = "")
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
paste(text_sample, collapse = " ")
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1,
skip = i-1, quote = "", quiet = T)
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
paste(text_sample, collapse = " ")
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample
sample_creation <- function(sample_size, path){
con <- file(path)
nrow_text <- as.numeric(system("wc -l < en_US.news.txt", intern = T))
text_sample <- c()
n_sample <- 0
for(i in 1:nrow_text){
test <- rbinom(1, 1, prob = c(0.5, 0.5))
if(test == 1){
n_sample <- n_sample + 1
readed_line <- scan(file = con, what = "char", nlines = 1,
skip = i-1, quiet = T)
text_sample <- c(text_sample, readed_line)
}
if(n_sample == sample_size) break
}
close(con)
paste(text_sample, collapse = " ")
}
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample
sample <- sample_creation(sample_size = 10, path = "~/Documents/datasciencecoursera/Capstone Project/data/en_US/en_US.news.txt")
sample
